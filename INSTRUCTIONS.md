# SPE-PROJECT UPGRADE — MASTER GUIDE
# ===================================
# Antigravity instructions:
#   REPLACE if file exists, CREATE if it does not
#   All paths are relative to spe-project/ root
#
# After applying all files, run:
#   pip install -r requirements.txt
#   docker-compose down -v
#   docker-compose up -d
#   git add . && git commit -m "feat: v2 full upgrade" && git push origin main
